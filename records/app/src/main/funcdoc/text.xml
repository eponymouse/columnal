<?xml version="1.0"?>
<functionDocumentation xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
                       xsi:noNamespaceSchemaLocation="funcdoc.xsd"
                       namespace="text">
    <function name="text length">
        <argType>Text</argType>
        <returnType>Number</returnType>
        <description>
            <p>Gets the length of the given text.</p>
            <p>Technical note: this is actually the number of Unicode codepoints in the text.  For English text without accents, one character is one codepoint.  If you have accents or non-English characters in your text, what looks like one character may be made up of multiple codepoints.</p>
        </description>
        <example>
            <input>"hello"</input>
            <output>5</output>
        </example>
        <example>
            <input>"a b."</input>
            <output>4</output>
        </example>
        <example>
            <input>""</input>
            <output>0</output>
        </example>
    </function>
    
    <function name="replace all">
        <argType>(Text, Text, Text)</argType>
        <returnType>Text</returnType>
        <description>
            <p>Replaces all occurrences of the first text with the second text, within the third text</p>
            <p>If the first text is empty, no replacement is performed.  If any occurrences overlap, the earlier part is replaced first.</p>
        </description>
        <example>
            <input>("at", "og", "The cat in the hat")</input>
            <output>"The cog in the hog"</output>
        </example>
        <example>
            <input>("oo", "a", "Mooo!")</input>
            <output>"Mao!"</output>
        </example>
        <example>
            <input>("", "x", "Orange")</input>
            <output>"Orange"</output>
        </example>
    </function>
    
    <function name="join text">
        <argType>[Text]</argType>
        <returnType>Text</returnType>
        <description>
            <p>Joins a list of text values together as one text item.</p>
            <p>If you want to add a separator between each text item, use <link function="join text with"/> instead.</p>
        </description>
        <equivalence>
            <forany name="x">[Text]</forany>
            <lhs>@call @function join text(x)</lhs>
            <rhs>@call @function join text with(x, "")</rhs>
        </equivalence>
        <equivalence>
            <forany name="x">[Text]</forany>
            <lhs>@call @function join text(x)</lhs>
            <rhs>@call @function combine(x, (? ; ?))</rhs> 
        </equivalence>
        <example>
            <input>["a", "b", "c"]</input>
            <output>"abc"</output>
        </example>
        <example>
            <input>["Hello", " ", "Moon"]</input>
            <output>"Hello Moon"</output>
        </example>
        <example>
            <input>[]</input>
            <output>""</output>
        </example>
    </function>

    <function name="join text with">
        <argType>([Text], Text)</argType>
        <returnType>Text</returnType>
        <description>
            <p>Joins a list of text values together as one text item, inserting a separator between each.</p>
        </description>
        <equivalence>
            <forany name="x">[Text]</forany>
            <lhs>@call @function join text with(x, "")</lhs>
            <rhs>@call @function join text(x)</rhs>
        </equivalence>
        <equivalence>
            <forany name="x">[Text]</forany>
            <forany name="sep">Text</forany>
            <lhs>@call @function join text with(x, sep)</lhs>
            <rhs>@call @function combine(x, (? ; sep ; ?))</rhs>
        </equivalence>
        <example>
            <input>(["a", "b", "c"], "+")</input>
            <output>"a+b+c"</output>
        </example>
        <example>
            <input>(["Blanks", "", "Count"], ";")</input>
            <output>"Blanks;;Count"</output>
        </example>
        <example>
            <input>(["Hello", " ", "Moon"], "-")</input>
            <output>"Hello- -Moon"</output>
        </example>
        <example>
            <input>(["Hi"], ",")</input>
            <output>"Hi"</output>
        </example>
        <example>
            <input>([], ",")</input>
            <output>""</output>
        </example>
    </function>
    <function name="trim">
        <argType>Text</argType>
        <returnType>Text</returnType>
        <description>
            <p>Removes white space from beginning and end of the text.</p>
        </description>
        <equivalence>
            <forany name="x">Text</forany>
            <lhs>@call @function trim(@call @function trim(x))</lhs>
            <rhs>@call @function trim(x)</rhs>
        </equivalence>
        <example>
            <input>"No spaces  either side."</input>
            <output>"No spaces  either side."</output>
        </example>
        <example>
            <input>"  Several spaces    "</input>
            <output>"Several spaces"</output>
        </example>
        <example>
            <input>"^t Tabs and newlines also removed.^n"</input>
            <output>"Tabs and newlines also removed."</output>
        </example>
    </function>
</functionDocumentation>